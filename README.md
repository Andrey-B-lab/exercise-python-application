This is the exercise file for the following pipeline:

Jenkins Declarative Pipeline that build the Python application on server-1, build the Docker Image from it, upload to ECR, then run it as a POD on server-2 and expose it to port 443.
